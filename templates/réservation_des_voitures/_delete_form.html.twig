<form method="post" action="{{ path('app_r_servation_des_voitures_delete', {'id': r_servation_des_voiture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ r_servation_des_voiture.id) }}">
    <button class="btn">Delete</button>
</form>
