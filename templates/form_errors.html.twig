{# templates/form/form_theme.html.twig #}

{% block form_errors %}
    {% if errors|length > 0 %}
        {% for error in errors %}
            <span class="error-message">{{ error.message }}</span>
        {% endfor %}
    {% endif %}
{% endblock %}
