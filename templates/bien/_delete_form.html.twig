<form method="post" action="{{ path('app_bien_delete', {'refB': bien.refB}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bien.refB) }}">
    <button style="color: rgb(46, 149, 204);" class="btn">Delete</button>
</form>
